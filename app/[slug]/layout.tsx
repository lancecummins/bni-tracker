import { ReactNode } from 'react';

// Simple layout without authentication for slug-based team pages
export default function SlugLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}